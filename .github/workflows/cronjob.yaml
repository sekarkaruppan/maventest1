name: display current time every 3 hrs

# on:
#   schedule:
#     - cron: '1 * * * *' 

on:
  workflow_dispatch: 
  
env:
  env_var: ${{ vars.DAY_OF_THE_WEEK }}
  
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print current time every 3 hrs
        run:  |  
            echo "Current time:$(date)"
            echo ${{ vars.TESTVAR }}
            echo ${{ env_var.DAY_OF_THE_WEEK }}

